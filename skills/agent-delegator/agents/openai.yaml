interface:
  display_name: "Agent Delegator"
  short_description: "Delegate and resume work through the local ACP Gateway"
  default_prompt: "Use $agent-delegator to delegate this task through the local ACP Gateway."
dependencies:
  tools:
    - type: "mcp"
      value: "agent-acp"
      description: "Main-only local ACP Gateway control tools"
