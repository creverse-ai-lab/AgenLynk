# AgenLynk `src/` 소유권 분류

이 목록은 0.4.0 소유권 기준선이다. Phase 2에서 AgenLynk 전용 코드는 `sidecar/`로 이동했으며, `hybrid seam`은 그대로 유지할 목표 구조가 아니라 pinned Gateway 전환 전까지 결합을 한 adapter로 모아 둔 임시 경계다.

## Gateway fork

`agent_gateway` release artifact가 소유해야 하며 AgenLynk에서 최종 제거한다.

```text
acp-client.js
acp-version.js
agent-updates.js
artifacts.js
bootstrap.js
bounded-utf8.js
early-shutdown.js
frontdoor-context.js
gateway-api-version.js
gateway-daemon.js
gateway-service.js
gateway-source-monitor.js
guide.js
index.js
ndjson.js
process-environment.js
sessions.js
socket-flow.js
source-update.js
```

## AgenLynk sidecar

앱과 함께 버전이 움직이며 `Contents/Resources/sidecar`에서 실행한다.

```text
sidecar/src/local-agents/*
sidecar/src/local-monitor.js
sidecar/src/local-transcript.js
sidecar/src/projection/monitor-state.js
sidecar/src/server/monitor.js
```

이 namespace는 `sidecar/package.json`의 `@creverse-ai-lab/agenlynk-sidecar` 패키지와 `agenlynk-sidecar` bin으로 실행되며 Gateway와 별도의 version/build ID를 가진다.

## AgenLynk app runtime integration

Gateway 구현이 아니라 app packaging/install/activation 경계다. artifact 전환 후에도 역할을 검토해 유지하거나 Swift `GatewayRuntimeManager`로 옮긴다.

```text
build-release-manifest-cli.js
build-runtime-manifest-cli.js
fs-paths.js
runtime-installer-cli.js
runtime-installer.js
runtime-lock.js
runtime-manifest.js
runtime-pointer.js
runtime-smoke-check.js
runtime-staging.js
runtime-updater-cli.js
runtime-updater.js
verify-runtime-manifest-cli.js
```

## Hybrid seam

현재 sidecar와 Gateway fork가 함께 의존한다. PR 1에서는 이 결합을 `sidecar/src/gateway/legacy-adapter.js`로 집중하고, pinned 1.4.0 전환 때 public client 또는 app-owned service로 교체한다.

```text
acp-registry.js
agent-catalog.js
config.js
gateway-settings.js
installer.js
providers.js
socket-rpc.js
version.js
```

## 이동 규칙

- sidecar는 `LegacyGatewayAdapter` 밖에서 Gateway fork/private path를 import하지 않는다.
- app runtime integration은 Gateway source tree hash를 새로 정의하지 않는다. release manifest와 lock digest를 검증한다.
- `gateway_config`, agent catalog, app runtime update는 1.4.0 Gateway public wire라고 가정하지 않는다.
- `macos/scripts/build-app.sh`는 runtime seed 안에서도 `src/`와 `sidecar/`를 별도 namespace로 복사한다.
- 파일 삭제는 public client + pinned artifact + package smoke가 통과한 뒤에만 한다.
